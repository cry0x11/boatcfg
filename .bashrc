# If not running interactively, don't do anything
[[ $- != *i* ]] && return


# ----------------------------
# SOURCING EXTENSIONS
# ----------------------------
source ~/bash/prompt
source ~/bash/aliases
source ~/bash/variables
